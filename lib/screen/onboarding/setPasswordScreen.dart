import 'package:flutter/material.dart';

class setPasswoedScreen extends StatefulWidget {
  const setPasswoedScreen({super.key});

  @override
  State<setPasswoedScreen> createState() => _setPasswoedScreenState();
}

class _setPasswoedScreenState extends State<setPasswoedScreen> {
  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
